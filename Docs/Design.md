# 設計書: ESC/POS 週間スケジュールプリンター

## 概要
M5サイズの週間スケジュール（リフィル）を生成し、ESC/POSプリンタへ印刷するツール。
PowerShellによるデスクトップGUI版と、PHPによるブラウザ版を提供する。

## システム構成

### 対象環境
#### PowerShell版
- **OS**: Windows (PowerShell 5.1以上)
- **言語**: PowerShell
- **GUI**: Windows Forms
- **ターゲットプリンタ**: ESC/POSコマンド対応のネットワークプリンタ

#### PHP版
- **実行環境**: PHP 8.1以上 (mbstring拡張必須)
- **UI**: ブラウザ (HTML/CSS)
- **サーバー**: ビルトインサーバー (php -S) 等でローカル実行

### 主要コンポーネント
1. フロントエンド（PowerShell Forms / Web UI）
2. ESC/POSペイロード生成エンジン
3. ネットワーク印刷モジュール (TCP/IP)
4. 週間テンプレート生成器
5. 設定・データ永続化機構

## アーキテクチャ

### 1. データフロー
```
[ユーザー入力]
    ↓
[週間テンプレート生成] ←→ [保存ファイル読み込み]
    ↓
[プレビュー表示（編集可能）]
    ↓
[ESC/POSペイロード変換]
    ↓
[TCP/IP送信] → [プリンタ]
    ↓
[印刷内容をファイル保存]
```

## 機能仕様

### 1. 週間スケジュール生成機能
- **開始日**: 日付選択（デフォルト: 当週月曜日）
- **週の定義**: 月曜日始まり
- **テンプレート構造**:
  ```
  [空行]
  yyyy-MM-dd(Mon)
  [空行×3]
  [区切り線]
  ...（7日分）
  ```
- **土日の強調表示**: `!!yyyy-MM-dd(Sat)!!` 形式でマークアップ（印刷時に反転表示）

### 2. ESC/POSコマンド生成
#### サポートコマンド
| コマンド | 16進 | 説明 |
|---------|------|------|
| ESC @ | 1B 40 | プリンタ初期化 |
| FS C 1 | 1C 43 01 | 日本語フォント有効化 |
| ESC M 1 | 1B 4D 01 | フォントB（小） |
| ESC 3 n | 1B 33 28 | 行間設定（0x28=40） |
| GS ! 0x11 | 1D 21 11 | 文字サイズ2倍（オプション） |
| GS B 1/0 | 1D 42 01/00 | 反転ON/OFF |
| GS V A 3 | 1D 56 41 03 | 用紙カット（オプション） |

#### マークアップ処理
- `!!テキスト!!`: 反転表示（GS B 1 → テキスト → GS B 0）
- 正規表現: `(?s)!!(.+?)!!` でパース

### 3. ネットワーク印刷
- **プロトコル**: TCP/IP
- **既定接続先**: 172.16.20.31:9100
- **タイムアウト**: 設定可能（デフォルト5秒）

### 4. データ永続化
#### 週間バックアップ
- **保存形式**: `m5-weekly-refill-yyyy-MM-dd.bak.txt` (UTF-8)
- **保存場所**: 実行ファイルと同じディレクトリ（または各版のディレクトリ）
- **保存タイミング**: 印刷成功時、または明示的な保存操作時

#### IP設定 (PHP版のみ)
- **ファイル名**: `ip-addresses.json`
- **形式**: JSON配列
  ```json
  {
    "ips": ["172.16.20.31", "192.168.0.10"],
    "selected": "172.16.20.31"
  }
  ```

### 5. UIコンポーネント (共通)
- **IP/Port/Timeout**: プリンタ接続設定
- **2倍サイズ/カット**: ESC/POSオプション
- **プレビュー編集モード**: テンプレート内容の直接編集
- **マーカー表示**: `!!` 記号の表示/非表示トグル
- **週操作**: 前週/次週/適用ボタン

## 文字エンコーディング
- **内部処理**: UTF-8
- **送信時**: Shift_JIS (CP932)
- **改行**: LF (`\n`) に統一して送信（末尾LF保証）

## 開発・運用
- **仕様駆動開発**: `Docs/Design.md` および `AGENT.md` に基づき実装。
- **ディレクトリ構成**:
  - `powershell版/`: Windowsデスクトップ用
  - `PHP版/`: ブラウザ利用・サーバー用
  - `Docs/`: 設計ドキュメント
